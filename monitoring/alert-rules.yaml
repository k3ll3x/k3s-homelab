groups:
- name: k3s-alerts
  rules:
  - alert: K3sNodeDown
    expr: up{job="node-exporter"} == 0
    for: 2m
    labels:
      severity: critical
    annotations:
      summary: "K3s node {{ $labels.instance }} down"
